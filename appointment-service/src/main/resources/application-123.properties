server.port=8085
spring.application.name=appointment-service

spring.datasource.url=jdbc:h2:mem:appointmentsA;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# sem seeds por agora
spring.sql.init.mode=never

hap.p2p.peers=http://localhost:8085,http://localhost:9085

# URLs dos serviços externos (OBRIGATÓRIOS para o serviço arrancar sem nulls)
hap.services.physicians=http://localhost:8084,http://localhost:9084
hap.services.patients=http://localhost:8081,http://localhost:8082

external.auth.user=admin
external.auth.pass=admin123

